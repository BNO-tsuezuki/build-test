'use strict';

describe('Directive: d3Map', function () {

  // load the directive's module
  beforeEach(module('evoserverApp'));

  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
